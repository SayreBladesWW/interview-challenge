# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /:symbol/marketcap/:date            controllers.MarketCapControllerImpl.get(symbol,date)

